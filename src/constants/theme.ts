export enum ComponentTheme {
  Grey = 'grey',
  Light = 'light',
}

export enum TextOverflow {
  Wrap = 'wrap',
  Truncate = 'truncate',
}

export enum PaddingInput {
  Default = 'default',
  Moderate = 'moderate',
  Minimal = 'minimal',
  None = 'none',
}
